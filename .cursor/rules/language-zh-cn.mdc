---
description: 所有对话问答和生成的文档均使用中文
alwaysApply: true
---

# 语言规范：中文

本项目所有对话中的问答、解释、说明，以及生成的所有文档（Markdown、注释、提交信息、ADR、Issue、Spec 等），均使用**中文**。

## 适用范围

- 与用户的对话回复
- 生成或修改的文档（`.md`、`.mdc`、ADR、Spec、Issue、README 等）
- 代码中的注释（注释语言保持与项目其他注释一致；若项目已有中文注释，则中文；否则跟随本规则）
- 提交信息（`git commit` message）

## 不适用范围

- 标识符：变量名、函数名、类名、文件名等仍使用英文
- 第三方库的 API 调用、日志 key、JSON 字段名等技术性标识符保持英文
- 用户明确要求使用其他语言时，以用户要求为准
