# Peer-instance overrides for `pnpm run dev:peer`. Tracked in git; everything
# here is non-secret. Each value overrides the matching key from .env /
# .env.development when --env-file=.env.peer is passed explicitly.

SIDECAR_TOKEN=1A9FBFF8-863A-10A1-061B-709A2C58E2D3
SIDECAR_HOST=127.0.0.1
SIDECAR_PORT=4322
SIDECAR_STORE_DIR=./.peer-sidecar-store
SIDECAR_SWARM_PORT=0
SIDECAR_LOG_LEVEL=info
SIDECAR_BOOTSTRAP=127.0.0.1:9000
